#!usr/bin/env ruby

require 'makefolio'
require 'pathname'

src_path = Pathname.new(Dir.pwd).join '/_src'

if src_path.exist?
  site = Makefolio::Site.new(src_path.to_s)
  site.generate
  puts 'Site generated'
else
  puts 'No source folder found!'
end
